import { View, Text } from 'react-native'
import React from 'react'

const SimpleGrid = () => {
  return (
    // <View className="flex flex-row flex-wrap">
    //   {gridData.map((item, index) => (
    //     <View key={item.id} className={`w-[${gridWidth}px]`}>
    //       <View className={`m-[4px] p${index % 2 == 0 ? 'l' : 'r'}-[12px]`}>
    //       {/* <View className={`m-[4px] ${index % 2 == 0 ? 'pl-[12px]' : 'pr-[12px]'}`}> */}
    //         <TouchableOpacity className="border-gray-200 border-[1px] rounded-lg py-2.5 px-2.5">
    //           <TextB className="text-sm" numberOfLines={1} ellipsizeMode='tail'>{ item.name }</TextB>
    //           <TextR className="text-xs text-gray-400">3 Brewster Rd, Newark</TextR>
    //         </TouchableOpacity>
    //       </View>
    //     </View>
    //   ))}
    // </View>
  )
}

export default SimpleGrid